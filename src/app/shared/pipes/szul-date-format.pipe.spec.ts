import { SzulDateFormatPipe } from './szul-date-format.pipe';

describe('SzulDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SzulDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
