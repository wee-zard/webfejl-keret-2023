import { PipeDictatingFormatPipe } from './pipe-dictating-format.pipe';

describe('PipeDictatingFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeDictatingFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
